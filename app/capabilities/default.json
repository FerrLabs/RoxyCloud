{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The main window listens to events and to files dropped on it",
  "windows": ["main"],
  "permissions": ["core:default"]
}
