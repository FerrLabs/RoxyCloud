{{- if .Values.ingress.enabled }}
{{- if not .Values.ingress.host }}
{{- fail "set ingress.host when ingress.enabled is true" }}
{{- end }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "roxycloud.fullname" . }}
  labels:
    {{- include "roxycloud.labels" . | nindent 4 }}
  {{- with .Values.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- with .Values.ingress.className }}
  ingressClassName: {{ . }}
  {{- end }}
  {{- if .Values.ingress.tls.enabled }}
  tls:
    - hosts:
        - {{ .Values.ingress.host | quote }}
      secretName: {{ required "set ingress.tls.secretName, or turn ingress.tls.enabled off" .Values.ingress.tls.secretName }}
  {{- end }}
  rules:
    - host: {{ .Values.ingress.host | quote }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ include "roxycloud.fullname" . }}
                port:
                  number: {{ .Values.service.port }}
{{- end }}
